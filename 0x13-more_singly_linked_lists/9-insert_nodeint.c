#include "lists.h"

/**
 * sum_listint - check the code
 * @head: a
 * Return: Always 0.
 */
listint_t *insert_nodeint_at_index(listint_t **head, unsigned int idx, int n)
{
	listint_t *nav = *head;

	while (nav) {

	}
	return (NULL);
}
